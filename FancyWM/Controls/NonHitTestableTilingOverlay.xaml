<UserControl x:Class="FancyWM.Controls.NonHitTestableTilingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FancyWM.Controls"
             xmlns:viewmodels="clr-namespace:FancyWM.ViewModels"
             xmlns:theme="clr-namespace:FancyWM.ThemeEngine.Wpf;assembly=FancyWM.ThemeEngine"
             d:DataContext="{d:DesignInstance Type=viewmodels:TilingOverlayViewModel}"
             mc:Ignorable="d"
             IsHitTestVisible="False"
             UseLayoutRounding="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <Canvas>
        <Rectangle
            Stroke="{theme:CssResource window:focus/border-color}"
            StrokeThickness="{theme:CssResource window:focus/border-width}"
            RadiusX="{theme:CssResource window:focus/border-radius}"
            RadiusY="{theme:CssResource window:focus/border-radius}"
            Effect="{theme:CssResource window:focus/filter}"
            Width="{Binding FocusRectangle.Width}"
            Height="{Binding FocusRectangle.Height}"
            Canvas.Left="{Binding FocusRectangle.Left}"
            Canvas.Top="{Binding FocusRectangle.Top}">
            <Rectangle.Style>
                <Style>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=IsFocusRectangleVisible}" Value="True">
                            <DataTrigger.Setters>
                                <Setter Property="UIElement.Visibility" Value="Visible" />
                            </DataTrigger.Setters>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=IsFocusRectangleVisible}" Value="False">
                            <DataTrigger.Setters>
                                <Setter Property="UIElement.Visibility" Value="Collapsed" />
                            </DataTrigger.Setters>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Rectangle.Style>
        </Rectangle>

        <Rectangle
            Stroke="{theme:CssResource panel.preview/border-color}"
            StrokeThickness="{theme:CssResource panel.preview/border-width}"
            RadiusX="{theme:CssResource panel.preview/border-radius}"
            RadiusY="{theme:CssResource panel.preview/border-radius}"
            Fill="{theme:CssResource panel.preview/background}"
            Effect="{theme:CssResource panel.preview/filter}"
            Opacity="0"
            Width="{Binding PreviewRectangle.Width}"
            Height="{Binding PreviewRectangle.Height}"
            Canvas.Left="{Binding PreviewRectangle.Left}"
            Canvas.Top="{Binding PreviewRectangle.Top}">
            <Rectangle.Style>
                <Style>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=IsPreviewRectangleVisible}" Value="True">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.1" />
                                        <ObjectAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetProperty="(UIElement.Visibility)">
                                            <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Visible}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.1" />
                                        <ObjectAnimationUsingKeyFrames BeginTime="0:0:0.1" Storyboard.TargetProperty="(UIElement.Visibility)">
                                            <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Collapsed}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Rectangle.Style>
        </Rectangle>

        <ItemsControl ItemsSource="{Binding Path=WindowElements}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Rectangle
                        Stroke="{theme:CssResource window.preview/border-color}"
                        StrokeThickness="{theme:CssResource window.preview/border-width}"
                        RadiusX="{theme:CssResource window.preview/border-radius}"
                        RadiusY="{theme:CssResource window.preview/border-radius}"
                        Effect="{theme:CssResource window.preview/filter}"
                        Opacity="0"
                        Width="{Binding ComputedBounds.Width}"
                        Height="{Binding ComputedBounds.Height}">
                        <Rectangle.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=IsPreviewVisible}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.1" />
                                                    <ObjectAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetProperty="(UIElement.Visibility)">
                                                        <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Visible}"/>
                                                    </ObjectAnimationUsingKeyFrames>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.1" />
                                                    <ObjectAnimationUsingKeyFrames BeginTime="0:0:0.1" Storyboard.TargetProperty="(UIElement.Visibility)">
                                                        <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Collapsed}"/>
                                                    </ObjectAnimationUsingKeyFrames>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Rectangle.Style>
                    </Rectangle>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding Path=ComputedBounds.Left}" />
                    <Setter Property="Canvas.Top" Value="{Binding Path=ComputedBounds.Top}" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas IsItemsHost="True" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <ItemsControl ItemsSource="{Binding Path=WindowElements}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Rectangle
                        IsHitTestVisible="False"
                        Opacity="{Binding RevealHighlightOpacity}"
                        Width="{Binding ActionsBounds.Width}"
                        Height="8"
                        Margin="0,0,0,0"
                        VerticalAlignment="Top"
                        HorizontalAlignment="Stretch">
                        <Rectangle.Fill>
                            <RadialGradientBrush
                                GradientOrigin="0.5,0" Center="0.5,0"
                                RadiusX="0.5" RadiusY="0.5">
                                <GradientStop Color="{DynamicResource SystemAccentColor}" Offset="0" />
                                <GradientStop Color="Transparent" Offset="1.0" />
                            </RadialGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding Path=ActionsBounds.Left}" />
                    <Setter Property="Canvas.Top" Value="{Binding Path=ActionsBounds.Top}" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas IsItemsHost="True" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </Canvas>
</UserControl>
